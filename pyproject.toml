[tool.pyright]
extraPaths = ["backend"]
executionEnvironments = [
  { root = ".", extraPaths = ["backend"] }
]

[tool.pytest.ini_options]
pythonpath = ["backend"]
testpaths = ["backend/tests", "tests"]
